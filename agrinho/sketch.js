//Load the image.
function preload() {
  img = loadImage('pixil-frame-0 (5).png');
}

function setup() {
  createCanvas(500, 500);

  background(500);

  // Draw the image.
  image(img, 0, 0);

  describe('An image of the underside of a rgb(0,255,255) umbrella with a gridded ceiling above.');
  }

//load the image.
function preload() {
  img = loadImage('pixil-frame-0.png')
}
